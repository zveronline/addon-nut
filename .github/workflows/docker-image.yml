name: Docker Image CI

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
#  IMAGE_NAME: ${{ secrets.DOCKER_HUB_REPOSITORY }}
   IMAGE_NAME: ${{ github.repository }}
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image amd64
        uses: docker/build-push-action@v6
        with:
          build-args: |
            MAINTAINER=${{ github.repository_owner }}
            BRANCH=${{ github.ref_name }}
            SOURCE_COMMIT=${{ github.sha }}
          context: .
          platforms: |
            linux/amd64
          file: ./nut/Dockerfile-amd64
          tags: | 
            ghcr.io/${{ env.IMAGE_NAME }}/amd64:0.17.0
            ghcr.io/${{ env.IMAGE_NAME }}/amd64:stable
          push: true

      - name: Build and push Docker image arm64
        uses: docker/build-push-action@v6
        with:
          build-args: |
            MAINTAINER=${{ github.repository_owner }}
            BRANCH=${{ github.ref_name }}
            SOURCE_COMMIT=${{ github.sha }}
          context: .
          platforms: |
            linux/aarch64
          file: ./nut/Dockerfile-arm64
          tags: | 
            ghcr.io/${{ env.IMAGE_NAME }}/aarch64:0.17.0
            ghcr.io/${{ env.IMAGE_NAME }}/aarch64:stable
          push: true